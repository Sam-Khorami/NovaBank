export enum UserRoleEnum {
    USER = "user",
    ADMIN = "admin",
    SUPERADMIN = "superAdmin"
}

export enum WalletStatusEnum {
    Active = "active",
    Closed = "closed",
    Blocked = "blocked"
}